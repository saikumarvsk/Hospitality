pipeline{
  agent any
  stages{
    stage('integration')
    {
      steps{
        git 'https://github.com/saikumarvsk/Hospitality.git'
      }
    }
    stage("Sonarqube Analysis "){
            steps{
                withSonarQubeEnv('sonarscanner') {
                    sh ''' $SCANNER_HOME/bin/sonarscanner -Dsonar.projectName=Hospitality \
                    -Dsonar.projectKey=Hospitality '''
        }
    }
}

  }
}
